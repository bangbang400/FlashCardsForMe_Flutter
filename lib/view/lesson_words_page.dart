import 'package:flutter/material.dart';

class LessonWordsPage extends StatelessWidget {
  const LessonWordsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('レッスン'),
      ),
      body: Container(

      ),
    );
  }
}
